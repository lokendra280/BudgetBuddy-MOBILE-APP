class VoiceResult {
  final String title, category;
  final double amount;
  final bool isIncome;
  const VoiceResult({
    required this.title,
    required this.category,
    required this.amount,
    required this.isIncome,
  });
}
